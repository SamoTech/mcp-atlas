{
  "version": 2,
  "builds": [
    {
      "src": "web/package.json",
      "use": "@vercel/next",
      "config": {
        "rootDirectory": "web"
      }
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "web/$1" }
  ]
}
